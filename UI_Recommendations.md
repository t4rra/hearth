- Renaming/relabeling UI elements
  - "Probe Kindle" -> "Reconnect Kindle"
  - "Sync Selected" -> "Sync to Kindle" 
    - Additionally, move this button to the bottom right of the program
  - "Refresh Selected" -> "Reload Collection"
  - "Remove from Kindle" -> "Remove Hearth Folder from Kindle"
- When the progress bar pops up, it shifts the whole UI because there was no space made for it - make sure the container that holds the progress bar/status text is always present and doesn't cause layout shifts when it becomes visible.
  - Please make the status bar more granular - I want to see the progress of book conversion/transfer in real time reflected in the progress bar
  - I know both KCC and calibre outputs can be parsed for progress information, so please use that to update the progress bar in real time
- Move the "Force Re-sync" button to the left of the "Sync to Kindle" button
- In the confirmation dialog for removing the Hearth folder from the Kindle, while still using the native system dialog, change the "delete" button so that it's red 